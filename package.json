{
  "name": "la-cocina-de-la-turca",
  "version": "1.1.0",
  "description": "Sitio web de La Cocina De La Turca, comida árabe y judía casera (HTML + SCSS, sin frameworks).",
  "private": true,
  "scripts": {
    "build:css": "sass scss/estilos.scss css/estilos.css --no-source-map --style=expanded",
    "watch:css": "sass --watch scss/estilos.scss:css/estilos.css --no-source-map --style=expanded"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.105.0"
  }
}
